/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "localhost" ,
        "powercem-backend-production.up.railway.app"
      ],
    },
  };
  
  export default nextConfig;